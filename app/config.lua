config = {
    -- window
    updates_per_sec = 60,
    window_width = 1280,
    window_height = 720,
    window_name = "Pine Engine",

    -- camera
    cam_fov = 10,
    cam_projection = 1,     -- 0 = 3D, 1 = 2D (ortho)
    cam_start_z = 1,       -- how far back the camera starts
}
